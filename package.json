{
  "name": "grocerun-monorepo",
  "version": "1.0.0-alpha.0",
  "private": true,
  "packageManager": "npm@10.9.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "turbo format"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "turbo": "^2.7.3",
    "typescript": "^5.3.3",
    "z-schema": "^12.1.0"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0",
    "prisma": "^7.5.0"
  }
}
